{{- /*
PgBouncer is configured at container startup from environment variables sourced
from the existing `moio-greenfield-secrets` Secret. No ConfigMap is rendered so
connection settings are not exposed in plain config.
*/ -}}
