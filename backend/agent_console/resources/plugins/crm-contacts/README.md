# CRM Contacts Plugin

Provides CRM contact lookup/update capabilities for agent workflows.

## Required Setup

- Tenant config: `base_url`
- Tenant credential: `crm_api_key`

## Assets

- `openapi.json` is used by the plugin resource index.

